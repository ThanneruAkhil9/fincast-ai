# FinCast AI — Requirements
# Install with: pip install -r requirements.txt

# Core
pandas>=2.0.0
numpy>=1.24.0
duckdb>=0.9.0

# Forecasting (free)
statsmodels>=0.14.0

# Anomaly detection (free)
scikit-learn>=1.3.0

# Dashboard
streamlit>=1.30.0
plotly>=5.18.0

# Optional: LLM (free tier)
# Set GROQ_API_KEY env variable to enable LLM narratives
# groq>=0.4.0     ← only needed if using groq SDK instead of urllib
